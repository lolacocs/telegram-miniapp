import React from "react";
import ReactDOM from "react-dom/client";
import Day1 from "./Day1"; // импорт твоего компонента

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <Day1 />
  </React.StrictMode>
);
